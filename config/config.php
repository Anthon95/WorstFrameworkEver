<?php

    /* configuration file */

    /* default action */
    $config['defaults']['action'] = "home";
    $config['defaults']['template'] = "home";

    /* we start our templates */
    $smarty = new Smarty();
